{
  "name": "engagement-game",
  "version": "0.1.0",
  "private": true,
  "description": "Whac-a-mole go/no-go attention task for fMRI (Song Lab). Vanilla ES modules, no dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8000 --directory src"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
